function reversedChars(a, b, c) {
    let array = [];
    array.push(a);
    array.push(b);
    array.push(c);

    console.log(array.reverse().join(' '));
}

reversedChars('A', 'B', 'C');
reversedChars('1', 'L', '&');